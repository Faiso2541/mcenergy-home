# คู่มือขึ้นเว็บด้วย GitHub Pages (ไม่ต้องมี server)

## ภาพรวม

```
Customer Home Report/
├── index.html              ← หน้าเว็บหลัก (ใช้ไฟล์นี้)
├── reports.json            ← รายการรายงานทั้งหมด (สร้างอัตโนมัติ)
├── reports/
│   ├── 3M Innovation/      ← ชื่อโฟลเดอร์ = ชื่อลูกค้า
│   │   └── 2026-08-12_ตรวจเช็ค Chiller (PM).pdf
│   └── Colgate GDC/
│       └── 2026-07-05_Service Report.pdf
├── สร้างรายการรายงาน.bat   ← ดับเบิลคลิกเพื่ออัปเดต reports.json
└── tools/make-reports-json.ps1
```

เว็บแบบ static อ่านชื่อไฟล์ในโฟลเดอร์เองไม่ได้ จึงต้องมี `reports.json` เป็นสารบัญ — สร้างได้ 2 วิธี (เลือกอย่างใดอย่างหนึ่ง)

---

## ขั้นตอนที่ 1 — เตรียมไฟล์ PDF

1. สร้างโฟลเดอร์ `reports/<ชื่อลูกค้า>/` ให้ตรงกับชื่อในระบบ เช่น `reports/Colgate GDC/`
2. เอา PDF ที่ Save จาก Online Form ไปวาง ตั้งชื่อแบบนี้จะดีที่สุด

```
2026-08-12_ตรวจเช็ค Chiller (PM).pdf
└─ วันที่ ─┘└──── ชื่อรายงาน ────┘└ประเภท┘
```

ถ้าไม่ใส่วันที่หน้าชื่อไฟล์ ระบบจะใช้วันที่แก้ไขไฟล์ล่าสุดแทน

## ขั้นตอนที่ 2 — สร้าง reports.json

**วิธี A (แนะนำ):** ดับเบิลคลิก `สร้างรายการรายงาน.bat` → สแกนโฟลเดอร์ทั้งหมดให้อัตโนมัติ

**วิธี B (ไม่ใช้สคริปต์):** เปิดเว็บ → ล็อกอิน Admin → จัดการระบบ → แท็บรายงาน PDF → กรอกทีละรายการ (ช่องลิงก์ใส่ `reports/Colgate GDC/ชื่อไฟล์.pdf`) → กด **ส่งออก reports.json** แล้วเอาไฟล์ไปวางข้าง `index.html`

## ขั้นตอนที่ 3 — อัปโหลดขึ้น GitHub

1. สมัคร/เข้า github.com → **New repository** → ตั้งชื่อ เช่น `customer-report` → เลือก **Public** → Create
2. ในหน้า repo กด **Add file → Upload files** แล้วลากไฟล์ทั้งหมดใส่ (ลากทั้งโฟลเดอร์ `reports` ได้เลย) → Commit changes
3. ไปที่ **Settings → Pages** → Source: `Deploy from a branch` → Branch: `main` / `/ (root)` → Save
4. รอ 1-2 นาที จะได้ลิงก์ `https://<ชื่อบัญชี>.github.io/customer-report/` ส่งให้ลูกค้าได้เลย

**ข้อจำกัดของ GitHub Pages**

| หัวข้อ | ขีดจำกัด |
|---|---|
| ไฟล์เดี่ยว | 100 MB (อัปโหลดผ่านหน้าเว็บได้ครั้งละไม่เกิน 25 MB) |
| ทั้งเว็บ | 1 GB |
| Bandwidth | 100 GB/เดือน |

## ขั้นตอนที่ 4 — เวลามีรายงานใหม่

1. วางไฟล์ PDF ลงโฟลเดอร์ลูกค้า
2. ดับเบิลคลิก `สร้างรายการรายงาน.bat`
3. อัปโหลดไฟล์ PDF ใหม่ + `reports.json` ขึ้น GitHub ทับของเดิม

---

## ⚠️ เรื่องความปลอดภัยที่ต้องรู้

รหัสเข้าถึง 6 หลักในเว็บนี้กันได้แค่ระดับ "ไม่ให้กดเข้าผิดบริษัท" เท่านั้น เพราะ

- รหัสทุกบริษัทอยู่ในไฟล์ HTML/เบราว์เซอร์ ใครเปิด View Source ก็เห็น
- ไฟล์ PDF บน GitHub Pages เป็นสาธารณะ ใครรู้ URL ตรงก็โหลดได้โดยไม่ต้องผ่านหน้าล็อกอิน

**ลดความเสี่ยงได้โดย**

1. ตั้งชื่อโฟลเดอร์ให้เดายาก เช่น `reports/colgate-gdc-a7f39c/` (เว็บยังทำงานปกติ)
2. ไม่ใส่ข้อมูลลับมากในรายงานที่ขึ้นเว็บสาธารณะ

**ถ้าต้องการความปลอดภัยจริง** ให้ย้ายไปใช้อย่างใดอย่างหนึ่ง

- **Cloudflare Pages + Cloudflare Access** — ฟรีถึง 50 ผู้ใช้ ลูกค้าล็อกอินด้วยรหัส OTP ที่ส่งเข้าอีเมลบริษัทตัวเอง เข้าถึงได้เฉพาะอีเมลที่อนุญาต
- **SharePoint / OneDrive** — แชร์โฟลเดอร์ให้เฉพาะอีเมลลูกค้ารายนั้น แล้วเอาลิงก์มาวางในเว็บ
